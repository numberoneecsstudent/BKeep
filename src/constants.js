import { AppConfig } from 'blockstack'

export const appConfig = new AppConfig(['store_write', 'publish_data'])
export const POSTS_FILENAME = 'posts.json';
export const YEARS_FILENAME = 'years.json';
